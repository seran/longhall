<div class="columns">
	<div class="column">
		<div class="columns">
			<div class="column">
				<div class="buttons">
					<%= link_to 'Back', :back, class: "button is-gray is-outlined" %>
				<%= link_to "Create project", project_new_path, class: "button is-primary is-outlined" %>
				</div>
			</div>
		</div>
		<div class="columns">
			<div class="column">
				<% @projects.each do |project| %>
					<%= project.name %>
				<% end %>				
			</div>
		</div>
	</div>
</div>